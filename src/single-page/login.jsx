import 'console-polyfill';
import React from 'react';
import ReactDOM from 'react-dom';
import Login from '../vendors/wj/view/user/login/login';

const rootEl = document.getElementById('root');
ReactDOM.render(<Login />, rootEl);
